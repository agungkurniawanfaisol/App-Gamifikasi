# =============================================================================
# docker-compose.traefik.yml — Traefik reverse proxy overlay (optional)
# Usage: docker compose -f docker-compose.yml -f docker-compose.traefik.yml up -d --build
#
# Hostinger Traefik runs with network_mode: host (no shared Docker network).
# Traefik discovers this container via the Docker socket and routes to its bridge IP.
#
# Requires:
#   - TRAEFIK_HOST in .env
#   - DNS A record pointing to your VPS
# =============================================================================

services:
  nextjs:
    labels:
      - traefik.enable=true
      - traefik.http.routers.gamifikasi-deeptest.rule=Host(`${TRAEFIK_HOST}`)
      - traefik.http.routers.gamifikasi-deeptest.entrypoints=websecure
      - traefik.http.routers.gamifikasi-deeptest.tls.certresolver=letsencrypt
      - traefik.http.services.gamifikasi-deeptest.loadbalancer.server.port=5174
      - traefik.http.routers.gamifikasi-deeptest-http.rule=Host(`${TRAEFIK_HOST}`)
      - traefik.http.routers.gamifikasi-deeptest-http.entrypoints=web
      - traefik.http.routers.gamifikasi-deeptest-http.middlewares=gamifikasi-deeptest-redirect
      - traefik.http.middlewares.gamifikasi-deeptest-redirect.redirectscheme.scheme=https
